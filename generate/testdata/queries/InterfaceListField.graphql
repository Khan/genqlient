query InterfaceListField {
  root {
    id
    name
    children {
      id
      name
    }
  }
  # @genqlient(pointer: true)
  withPointer: root {
    id
    name
    children {
      id
      name
    }
  }
}
