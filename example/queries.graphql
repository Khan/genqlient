# getViewer gets the current user's name.
query getViewer {
  viewer {
    MyName: name
  }
}

# getUser gets the given user's name.
query getUser($login: String!) {
  user(login: $login) {
    theirName: name
  }
}
